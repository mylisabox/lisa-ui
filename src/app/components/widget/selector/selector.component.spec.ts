/* tslint:disable:no-unused-variable */
import {CardComponent} from "./card.component";

describe('Component: Card', () => {
  it('should create an instance', () => {
    let component = new CardComponent(null, null);
    expect(component).toBeTruthy();
  });
});
