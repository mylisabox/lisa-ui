/* tslint:disable:no-unused-variable */
import {FocusDirective} from "./focus.directive";

describe('Directive: Focus', () => {
  it('should create an instance', () => {
    let directive = new FocusDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
